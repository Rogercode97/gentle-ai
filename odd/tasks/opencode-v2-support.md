# OpenCode V1 and V2 compatibility

## Objective and authorization
Support existing OpenCode V1 installations and released V2 (initial target 2.0.4) without silently migrating user configuration, permissions, credentials, or session data.
User approved implementation in a new worktree; no commit, push, PR, merge, release, or global installation is authorized.
Worktree: /Users/alanbuscaglia/work/gentle-ai-worktrees/opencode-v2-support
Branch: feat/opencode-v2-support; base: 5383ee5bbaec34948d66d76d51912c64595f8a4b.
File locator: odd/tasks/opencode-v2-support.md
Engram mirror: odd/opencode-v2-support/tasks (project gentle-ai).

## Problem and rationale
V2 deliberately replaces plugin/server APIs and terminal configuration. Our five server plugins and TUI plugin use V1 contracts. Our config/profile/telemetry readers only understand V1 keys; installer advice and CI do not prove V2 compatibility. Supported V1 config and Markdown assets remain accepted upstream, so a wholesale rewrite is unnecessary.

## Constraints
- Preserve V1 behavior and unrelated user configuration; recognize native V2 precedence without creating conflicting shadow settings.
- Keep review evidence, binding, schema, admission, consent, and bounded lifecycle owned by existing Go contracts.
- Preserve exact-answer consent checks; never grant from arbitrary free text or background launch acknowledgements.
- Isolate tests with temporary homes/config/data/service paths. Never launch against ambient user OpenCode state or reuse credentials.
- No global install, automatic data migration, remote execution, or network model calls.
- English artifacts. One writer at a time. Roughly 400 authored lines per task is advisory, not a hard cap.
- RDD is on (global); final candidate requires native assessment/consent, not one cycle per task.
- SDD is not selected. Do not create SDD phase artifacts.
- Runtime now permits worktree writes without escalation; scope remains this worktree only.

## Testing contract
Strict TDD: enabled by existing openspec/config.yaml (strict_tdd and testing.strict_tdd).
Runner: go test ./...
Observe focused RED before production edits, GREEN, then REFACTOR. Run relevant package tests per task. Final go test ./... and go vet ./..., plus isolated runtime conformance where available. Report unavailable runtime/provider proof honestly; source tests alone do not certify V2.

## Tasks
- [x] T1 — Add isolated V1/V2 compatibility fixtures and native config/model parsing. Cover provider/providers, agent/agents, model variants and V2 precedence without regressing V1. Check: go test ./internal/opencode ./internal/model.
- [x] T2 — Update profile/config management, telemetry fallback and MCP inspection to read/write the applicable format safely. Check: go test ./internal/components/sdd ./internal/components/telemetryruntime ./internal/agents/opencode ./internal/components/engram.
- [x] T3 — Port non-review server plugin lifecycle, model catalog, skill registry and telemetry to released V2 while retaining V1 support. Verify installed-package contracts, events, cleanup and location isolation without model requests.
- [ ] T4 — Port SDD dispatch/results/consent and RDD transport using exact V2 tool/hook contracts; preserve Go authority and fail closed when organic proof is unavailable. Check focused component tests and adapter minimality; bench corpus remains unchanged.
- [ ] T5 — Make installation/update/detection version-aware and port TUI plugin/config without silently changing requested branding placement. Verify both majors and ownership-safe updates.
- [ ] T6 — Add compatibility CI/runtime coverage for supported V1 and pinned V2; use isolated state and deterministic provider-free checks. Paid/provider-backed proof remains pending unless explicitly authorized.
- [ ] T7 — Run integrated checks, document supported/unsupported capabilities and migration path, assess final candidate and relay native consent when required.

## Acceptance
All advertised features have V1 regression evidence and V2 contract/runtime proof appropriate to their surface. No unsupported runtime capability is advertised based solely on version arithmetic or static mocks. Config preserves user choices. No authorized implementation may mutate ambient user state. The test-isolation incident below violated the installation boundary; the user accepted the observed installation as-is. No database mutation is claimed or authorized.

## Evidence and progress
- Upstream npm: @opencode/cli latest 2.0.4, 2026-09-16; 2.0.0 published 2026-09-11.
- Sources: https://opencode.ai/v2/docs/migrate-v1/ ; https://opencode.ai/v2/docs/build/plugins/migrate-v1/ ; https://opencode.ai/v2/docs/tools/ .
- Research found hidden worker discovery, free-form questions, shared-service location semantics and changed TUI slots require explicit proof.
- T1 complete: V1/V2 provider and agent snapshots; native model selectors (string/expanded, variant), canonical native precedence within a file, per-layer normalization before higher-priority overlays, malformed-field safety, catalog alias identity and native variants. Existing bare SplitModelSpec contract is unchanged.
- T1 RED: go test ./internal/opencode ./internal/model failed with undefined ParseModelReference and TestConfigV2Assignments/{native_string,native_expanded,native_wins}, TestConfigV2Providers failures before production changes. Cross-layer regression then failed with global native model shadowing project legacy model; legacy partial-model regression failed with lost tool/reasoning fields before refinement.
- T1 GREEN: go test ./internal/opencode ./internal/model passed (opencode 0.266s, model cached); git diff --check passed. Added table-driven and t.TempDir fixtures. No golden updates.
- T1 runtime harness: N/A for pure file/model parsers; OpenCode process was not launched. Whole-repository tests and V2 runtime certification remain for later tasks.
- T1 sources: https://opencode.ai/v2/docs/migrate-v1/ ; https://opencode.ai/v2/docs/models/ ; https://opencode.ai/v2/docs/agents/ . Config selectors use model; provider modelID is an API alias, not the catalog identity.
- T1 CodeGraph: target index initialized by parent; read-only explore failed with unable to open database file, so bounded filesystem reads were used.
- T2 complete: profile detection/deletion/cleanup across both maps; native model overlay writes and system prompt preservation; shared bounded telemetry assignment reads; native CodeGraph MCP detection and disabled precedence; native Engram command/overlay preserving unrelated settings. Kilocode retains its existing merge path.
- T2 root cause: the historical plural agents alias migration would move/delete genuine V2 agents. Native-only evidence now bypasses it; model-only ambiguous plural maps are conservatively preserved as native, while explicit V1-only prompt/permission/variant entries retain the historical migration. This is config-format detection, not runtime-version detection.
- T2 RED: initial focused tests failed for native profile precedence, telemetry assignment, CodeGraph native presence/disabled shadowing, and Engram native command; added RED for ambiguous plural format and preservation of unrelated native permission rules.
- T2 GREEN (final bytes): go test ./internal/components/sdd ./internal/components/telemetryruntime ./internal/agents/opencode ./internal/components/engram ./internal/opencode ./internal/model passed (sdd 185.141s; remaining packages cached). git diff --check passed. No golden changes.
- T2 runtime harness: N/A for config-only changes; no real OpenCode process or ambient installation was launched. Generated permission maps translate to native ordered rules and preserve unrelated existing rules; unsupported generated shapes fail closed. Runtime plugin and transport proof remains T3/T4.
- T2 rollback boundary: config_format helper/tests; sdd native overlay helper/tests and reader/merge changes; telemetry fallback reader/test; OpenCode MCP adapter/test; Engram native injection helper/test. No persisted user-data migration.
- Rollback: each task's code/tests are a coherent removable unit; no user-state migration is introduced.

## Next step
T5 isolated checks now pass; review the partial T5 implementation and staged T4 relay, then finish T6 runtime conformance. The user accepted the current home installation as-is; no further home changes or rollback are authorized.

T1/T2 parent readback and focused spot checks passed. T3 assets and focused checks are complete; T4-T7 remain; no whole-repository tests, V2 runtime certification or final native review yet. No commit created.

T3 policy decision resolved: user explicitly approved bounded ephemeral RAM-only correlation with limits and expiration on 2026-09-16. Session/message IDs must never be persisted or transmitted. This is a narrow V2-only exception; no retries, persistent queues or broader identity collection. Preserve V1 policy. Update privacy documentation and executable tests with the V2 implementation. Evidence: research/opencode-v2-telemetry-policy (#19637), released declarations under /tmp/ocv2-contracts. T3 implementation is complete below. T4 now has a staged opaque relay, but organic runtime proof and positive capability remain pending; T5 is partial for the TUI placement gap and T6-T7 remain pending.

- T2 parent-readback correction complete: shipped-overlay RED proved existing question deny and a narrower validator inspection deny became allow. Corrected last-match ordering using V1 deny/ask protection and action-scoped replacement ownership; stale unauthorized task allow stays denied, legitimate explore/inspection remains allowed, and repeated merges are byte-idempotent. An added idempotency RED caught and eliminated duplicate/reordered generated rules before closure.
- T2 corrected-candidate verification: required six-package command passed once after final code/import normalization (sdd 132.982s, telemetryruntime 1.920s, engram 5.879s, opencode 1.061s; agents/opencode and model cached). git diff --check passed. This supersedes the earlier T2 proof, not its scope. No human policy decision required: existing V1 merge protection/ownership is the reference.


### T3 — V2 non-review plugin assets
- Separate `internal/assets/opencode/plugins-v2/{telemetry-runtime,model-variants,skill-registry}.ts` use released Plugin.define/setup/cleanup contracts; existing V1 plugin assets are unchanged. Version-selected installation was added in T5. The existing model-variants producer is retained; no new cache consumer was introduced.
- Telemetry subscribes to exact-location start/end/failure events. User-authorized RAM correlation is capped at 256 bounded keys with ten-minute expiration and a 30-second idle sweep. Veto/disposal/stream completion clear state; native children retain existing caps and one-shot behavior. No persistent identities, queues, retries, transcript lookup or raw error logging.
- V2 envelope normalization preserves selected-model (not response) provenance, sanitizes effort, rejects identities/unknown fields, rejects selectedEffort on V1 even when null, and never consults ambient model assignments for V2. The stale normalizer comment demanding identity dedupe was removed; no normalizer behavior changed.
- Catalog snapshots/refreshes use location+workspace-specific hashed cache files without touching the legacy global cache. Released SharedEvents is lazy until iterator.next(); the initial subscription now starts before model.list. Registry refresh uses only ctx.location.directory, preserves project marker guards and kills owned children on disposal.
- RED: missing V2 assets; V2 envelopes discarded by the V1-only bridge; catalog snapshot before subscription produced no cache; nested token objects leaked sentinel identities before scalar validation. GREEN: focused V2 Go/Node harness tests pass for correlation bounds/expiration/veto/disposal/duplicates/unmatched/failure/location/workspace isolation, scalar-only output, no logging/persistence APIs, catalog reload/cache isolation and registry ownership/guard parity.
- Final verification: `go test ./internal/assets ./internal/components/telemetryruntime ./internal/components/sdd ./internal/telemetry` passed on final production bytes (assets 8.969s, telemetryruntime 1.099s, sdd 124.295s, telemetry cached). `git diff --check` passed. No golden changes.
- Runtime harness: provider-free Node strip-types harness runs actual asset logic with a stub Plugin.define and mocked SDK events/processes; it is not installed OpenCode runtime proof or full released-package typechecking. No tsc/dependency installation, global config, provider call or ambient OpenCode process was used. T6 owns exact-runtime conformance.
- Rollback: remove the three new V2 assets and their harness/envelope tests, revert the schema-specific bridge branch, privacy documentation section and stale-comment correction; retain unrelated T1/T2 config changes.
- Parent owns Engram mirror synchronization. Discovery #19659 records lazy subscription/scalar sanitization findings. No commits created.


### T4 — Initial partial slice: V2 SDD adapter and unavailable review guard (historical)
- Added separate V2 `sdd-task-result-artifacts.ts`: await hook registrations, use `subagent`/`input`/`id`, direct `session.get({sessionID})`, structured `result.output`, and location-scoped deletion cleanup. Root-confirmed preflight is preserved; V2 custom answers bind only to one exact offered label after trim/case folding. Running-child acknowledgements never count as final output. Empty/error results latch further phase dispatch; formatted subagent content is never parsed.
- Added `plugins-v2/opencode-review-transport.ts` as an explicitly **unavailable guard only**, not an implemented or advertised relay. It refuses before launch, overwrites unsafe advisory completion with typed unavailable, and owns no process, binding, prompt/schema materialization, result parsing or isolation path. T4 remains unchecked because this does not complete review transport.
- Follow-up read-only research (#19674) identified a feasible narrow boundary: `internal/cli/review_transport_capability.go` owns actual capability, while registered runtime inventory is not capability proof. `internal/cli/review_opencode_transport.go` already accepts raw output. Implement the thin relay there without a new runtime enum or special isolation path; T5 must not advertise major-blind V2 review support. Actual relay and T6 organic proof remain pending.
- RED: both V2 assets absent. GREEN: provider-free Node tests cover awaited registration, partial registration cleanup, closed-label/multiselect/custom-answer refusal, root-only authority, structured raw completed output, background/running acknowledgment, empty/error latching, deletion/disposal and review unavailable before/after hooks. Static minimality guard rejects spawn/binding/schema/capture/isolation additions to the review placeholder.
- Required review package is `internal/reviewerprovider`; `internal/reviewtransaction` was also checked for native semantic regression. Initial `go test ./internal/assets ./internal/components/sdd ./internal/reviewerprovider ./internal/reviewtransaction` passed the first three but failed reviewtransaction on existing macOS `/var` symlink authority checks (`maintenance authority component /var is unsafe`, store lock not a directory). No source workaround was made.
- Canonical-path rerun `TMPDIR=/private/tmp go test ./internal/assets ./internal/components/sdd ./internal/reviewerprovider ./internal/reviewtransaction` passed: assets 3.676s, sdd 120.250s, reviewerprovider 0.064s, reviewtransaction 440.680s. `git diff --check` and `git diff --exit-code -- bench` passed. V1 plugin bytes and all Go review semantics unchanged; no golden changes.
- Runtime proof limit: mocked SDK/Plugin.define harness only. No installed V2 dispatch/consent/reviewer-provider proof, no released-package typechecking, no native review transaction initiated, no provider calls or global installs.
- Rollback: remove only the two V2 T4 assets and `internal/assets/opencode_v2_sdd_test.go`; retain T1-T3. Parent owns mirror. Discovery #19665 records capability gap; #19666 records canonical TMPDIR requirement. No commits.


### T5 — Test-isolation incident (historical; resolved for resume)
- The delegated writer ran `go test ./internal/components/opencodeplugin ./internal/update/upgrade -run 'TestV2' -count=1`. New `TestV2CommunityUpgradeRefusesBeforeMutation` mocked version detection but omitted `openCodeHomeDir` and `execCommand` seams. It invoked existing `opencodePluginUpgrade` with package `opencode-sdd-engram-manage`, version `1.2.0`; the expected refusal instead returned nil after 1.64s.
- Existing production defaults resolve the real home config directory and execute bun/npm. Possible command: `bun add opencode-sdd-engram-manage@1.2.0 @opencode-ai/plugin@latest`, or npm install equivalent (potential legacy-peer-deps retry). Which command ran and actual user-state changes have not been observed; output was discarded by the test. Package manifests, lockfiles and node_modules may have changed. This exceeded authorized worktree-only scope.
- All writer execution/edits stopped at discovery. The parent subsequently disclosed and performed the authorized bounded inspection recorded below. Do not claim user state unchanged. The test has since been isolated and reviewed before execution.
- T5 was partial and unverified at the incident stop. Subsequent isolated verification is recorded below; no commits were created.

### Incident resolution and resume authorization
- Read-only inspection confirmed package.json/bun.lock updates at 14:09:11 Madrid, installed manager 1.2.0 and V1 plugin 1.18.31. Prior exact versions remain unknown; June lockfile is not reliable rollback evidence. Bounded dependency backup search found no usable copies.
- User explicitly accepted leaving installation as-is and resuming worktree after correcting test isolation. No home restoration or future home mutation authorized. Test fixes must replace filesystem roots and command seams before any execution; parent reads back first. Memory #19694.


### T5 — Partial: version-aware managed assets; TUI placement unavailable
- Added bounded executable `--version` evidence (three seconds, 4096-byte output) and explicit V1/V2/unknown selection. Unknown or unsupported versions stop before managed runtime mutation. Kilocode remains explicitly V1. Production SDD install/refresh and CLI telemetry reconciliation use the detected major; compatibility telemetry wrappers remain explicitly V1.
- V2 installs all four managed server assets, including the existing model-variants producer, from `plugins-v2`. Exact-byte ownership preflight preserves custom/nonregular V2 target files before any replacement. V1 refresh semantics remain unchanged. Telemetry ownership validates major-appropriate source provenance and supports rollback of a known V1-to-V2 replacement; no historical V2 digest is invented.
- New-install advice uses `npm install -g @opencode/cli@latest` with required scripts enabled, not the former package or unsupported Homebrew route. This is command planning only, not a global installation or automatic migration. SDK dependency selection distinguishes V1 `@opencode-ai/plugin` and released V2 `@opencode/plugin@2.0.4`.
- Community plugin install/update fails closed on V2 until its compatibility is proven. The branding plugin specifically refuses because released `plugin/package/dist/tui/context.d.ts` exposes `home.footer`/`home.footer.status`, not the V1 `home_logo` placement. No brand relocation or unsupported TUI configuration is written. T5 stays unchecked: this is not a completed TUI port or full V2 support.
- Pinned discovery source (commit `466b3e594d3df396a57340249590a70c5c358c8a`) confirms automatic server `.ts`/`.js` discovery in `plugin` and `plugins`: https://raw.githubusercontent.com/anomalyco/opencode/466b3e594d3df396a57340249590a70c5c358c8a/packages/core/src/plugin/source-directory.ts ; registration order: https://raw.githubusercontent.com/anomalyco/opencode/466b3e594d3df396a57340249590a70c5c358c8a/packages/core/src/config/plugin/source.ts . No extra `plugins[]` config entry or new cache consumer was introduced.
- Isolation correction: the new updater refusal test sets temporary HOME/USERPROFILE/XDG_CONFIG_HOME and `openCodeHomeDir`, replaces `execCommand` with a fatal-before-process fake, and restores all seams through cleanup. It never delegates to the original executor. Parent read back this patch before resuming tests; the other new T5 tests use temp roots/pure command planning and mocked version evidence. No further home inspection, writes or restoration were performed by the writer.
- RED: runtime selector and major-specific telemetry API were missing; V2 SDD initially selected legacy assets and unknown versions mutated temp paths; installer expectations exposed old package/scripts advice. After the safety fix, `TMPDIR=/private/tmp go test ./internal/update/upgrade -run '^TestV2CommunityUpgradeRefusesBeforeMutation$' -count=1` failed safely before the production refusal existed. GREEN: focused V2 install/upgrade tests passed (0.006s/0.010s), and `TMPDIR=/private/tmp go test ./internal/cli -run '^TestOpenCodeTelemetryStepUsesDetectedMajor$' -count=1` passed (0.043s).
- Final required and affected verification: `TMPDIR=/private/tmp go test ./internal/opencode ./internal/agents/opencode ./internal/components/sdd ./internal/components/telemetryruntime ./internal/assets ./internal/installcmd ./internal/components/opencodeplugin ./internal/update/upgrade` passed (0.938s, 0.024s, 113.127s, 2.204s, cached, 0.035s, 1.111s, 4.729s respectively). `git diff --check` and `git diff --exit-code -- bench` passed. No golden updates.
- Runtime proof limits: only isolated Go/Node tests and pinned contract/source inspection; no installed V2 runtime certification, provider invocation, whole-repository test/vet run, or final native review. Review transport remains explicitly unavailable. T4 relay and T6/T7 remain pending.
- Rollback: revert runtime selector, install advice, SDD/telemetry selection, TUI/community refusal, CLI caller wiring and accompanying T5 tests as one coherent unit; preserve T1-T4 assets/config changes. No user-state rollback is implied or authorized. Parent owns full Engram mirror synchronization. `skill_resolution: paths-injected`.


### T4 — Staged opaque V2 relay; organic capability still unavailable
- Replaced the initial guard-only V2 review asset with the existing Go-owned start/prompt/complete/result wire protocol. The adapter forwards opaque prompts/raw output or a transport error only; it does not parse bindings, schemas or reviewer JSON, reconstruct evidence, capture results, retry, or introduce a special system/session isolation path. V1 asset bytes are unchanged.
- Released hooks use subagent/input.agent/input.prompt/id. Background execution and reused child-session requests refuse before spawning. Only both a completed hook and structured completed child output are eligible for raw forwarding; running acknowledgements/tool failures send transport error, never wrapper prose. A shared instance-owned registry keys location/workspace/session/call/agent, waits for materialization across duplicate instances, refuses unmatched completions, and cleans owned children on deletion/disposal/start failure. Hook registrations are awaited with partial-registration cleanup.
- The actual Go capability boundary now accepts only detected V1, and unknown/V2 remain unavailable. The direct internal relay command also checks that boundary before reading stdin or inspecting authority. No environment enable toggle, new runtime identity, positive V2 advertisement, binding/admission/schema change, or review semantic rewrite was introduced. Version probes are bounded and use the existing test seam; repeated capability callers can repeat observations, with no stale cross-process cache.
- Generated OpenCode V2 review instructions replace legacy task/subagent_type spellings with subagent/agent through the existing Go renderer. V1/Kilocode rendering remains unchanged. The renderer does not grant runtime capability.
- RED: staged relay harness failed at the former unavailable guard; capability tests exposed major-blind advertisement; V2 rendering still named V1 fields. GREEN: focused tests passed after implementation. Expanded cases cover duplicate ownership/orphans, location/workspace separation, background/reused-session rejection, native startup failure, raw versus wrapper output, nonterminal/error projection, session deletion/disposal, awaited hooks, and partial registration cleanup. Direct V2/unknown relay refusal tests use an input reader that panics if touched, proving rejection precedes input work.
- Adapter-minimality guards reject provider binding/evidence/schema/capture/isolation/filesystem/retry additions rather than prohibiting the transport subprocess itself. The Node harness replaces spawn with a fake before setup, runs under temporary homes, and invokes no real Go relay/model/package manager. No goldens changed.
- Required checks passed: `TMPDIR=/private/tmp go test ./internal/assets ./internal/components/sdd` (5.910s, 112.962s); `TMPDIR=/private/tmp go test ./internal/cli -run 'TestOpenCode.*(Transport|HostOutput)'` (94.918s); `TMPDIR=/private/tmp go test ./internal/cli -run 'Test(ImmutableReviewRuntime|UnsupportedImmutableReviewTransport|OpenCodeV2TransportCapability)'` (0.167s). `git diff --check` and `git diff --exit-code -- bench` passed. The expensive whole reviewtransaction suite was not repeated: no native semantic code changed, and the earlier recorded suite remains the semantic baseline.
- T4 remains unchecked pending T6 organic shared-contract conformance, including real host before-hook refusal and inherited-instruction sentinel evidence. Mock wire success is not installed runtime proof. No paid/provider call, global installation, ambient home mutation, native review transaction, commit or delivery claim.
- Rollback this follow-up as one unit: V2 relay replacement/harness, `review_opencode_v2_capability_test.go`, the narrow capability/direct-entry checks, and `boundedreview.go` V2 spelling selection/test. Keep the earlier SDD V2 adapter and other completed task work. Parent owns full task mirror; discovery #19706. `skill_resolution: paths-injected`.

### T6 — Partial: released SDK typecheck and isolated host smoke
- Added `scripts/test-opencode-v2-contracts.sh` and an independent CI job. It creates a disposable canonical temp fixture, uses an allowlisted environment and separate empty npm config files, disables lifecycle scripts, and installs exact `@opencode/plugin@2.0.4`, TypeScript 5.8.2 and Node types 24.12.2. Direct package pins are exact; transitive dependency resolution is not lockfile-frozen. CI was authored, not remotely executed.
- Exact released-package check: `TMPDIR=/private/tmp bash scripts/test-opencode-v2-contracts.sh` passed. Its compiler command is `node node_modules/typescript/bin/tsc --noEmit --strict --target es2022 --module nodenext --moduleResolution nodenext --skipLibCheck assets/*.ts`. Dependency declaration internals are skipped; all five asset sources are checked against imported released API types. RED exposed possibly undefined event.location in the review adapter; optional access fixed it. Initial npm fixture setup rejected using /dev/null twice as config; separate empty config files corrected that without any installation outside the fixture.
- Downloaded public `@opencode/cli-darwin-arm64@2.0.4` with npm pack and scripts disabled, then extracted its binary without executing postinstall. Package integrity: `sha512-so/EVkXYKyj5KwGJgGQsf7rCKu1DuU/G7ugrzYNLl7aEKd7351td7XTlSRe48s9D/KwqvYaIRnREBQsqlTTT/g==`. Actual isolated --version output is `opencode v2.0.4`; a RED test demonstrated the selector rejected this released shape, then the parser was corrected.
- Added `scripts/test-opencode-v2-host.py <extracted-binary>`; it passed using the extracted binary. It creates fresh HOME/XDG/config/data/state/cache/tmp/project roots, passes an allowlisted child environment without provider credentials, bounds commands, and terminates private process groups. It verifies the real version and `api --standalone model.list` returning the exact fixture location and empty data array. This does not invoke a model or certify plugins.
- Actual runtime limits: a direct server started on loopback, but unauthenticated endpoint polling did not prove readiness. The subsequent standalone API smoke passed. `plugin.list` returned an empty array with both project and temporary global plugin directories; plugin activation was NOT established. Do not infer either successful activation or a confirmed discovery defect from that result. No scripted-provider subagent dispatch, inherited-instruction sentinel, host before-hook refusal or end-to-end Go admission proof was obtained. T6 remains unchecked and V2 review stays unadvertised.
- Coexistence correction: PATH may point to V1 while the active host is V2. The V2 relay now declares `gentle-ai.opencode-relay/v2-staged` through `GENTLE_AI_OPENCODE_RELAY_CONTRACT` on its child and the released shell.create.before environment hook. Native capability rejects every nonempty declaration before PATH probing: no value enables capability. RED/GREEN covers coexisting V1 and preserves unrelated shell environment. Pinned `packages/core/src/shell.ts` lines 239-277 confirms hook mutation reaches the shell process; organic activation of this hook remains unproven, so absent/inactive plugin host identity remains a limitation. V2 rendered instructions now explicitly say unavailable and forbid reviewer launch until conformance.
- Focused final checks passed: `TMPDIR=/private/tmp go test ./internal/assets ./internal/opencode ./internal/components/telemetryruntime` (6.654s, 0.367s, 0.810s); `TMPDIR=/private/tmp go test ./internal/cli -run '^TestOpenCodeV2Transport' -count=1` (0.016s); `TMPDIR=/private/tmp go test ./internal/components/sdd -run 'TestOpenCodeV2ReviewToolRendering|TestReviewExecutionContract' -count=1` (0.010s). `bash -n scripts/test-opencode-v2-contracts.sh`, `git diff --check`, and bench unchanged passed. Earlier full V1 asset/package regression remains recorded; no V1 asset edits, golden updates, review semantic changes or expensive reviewtransaction rerun.
- Rollback this slice: remove the two conformance scripts/CI job, revert optional-location/parser corrections and coexistence declaration/disclosure tests and wiring. No user installation/data migration or home restoration is involved. Remaining work: determine actual plugin activation requirements in the isolated released host, then prove foreground dispatch, permissions and inherited-instruction behavior against a loopback scripted provider before considering advertisement. Parent owns full mirror. `skill_resolution: paths-injected`.

### T6 follow-up — Durable activation and actual negative shell hook proof
- Independent read-only probe #19716 resolved the empty inventory: plugin.list returns current inventory while activation runs asynchronously. The prior one-shot API exited before activation; it was neither successful setup evidence nor a discovery defect.
- `scripts/test-opencode-v2-host.py` now requires `<2.0.4-binary> <isolated-node_modules>` and copies released SDK dependencies into fresh disposable fixtures. Global and project discovery run separately to avoid duplicate plugin IDs. A persistent `serve --stdio --port 0` process uses a live stdin lease, fixture-only password and authenticated loopback HTTP; all HOME/XDG/config/data/state/cache/tmp paths and process environment remain isolated. Polls are bounded and the entire owned process group is cleaned on success/failure.
- Actual released-host GREEN: all five Gentle plugin IDs become active in BOTH fresh scopes. Actual shell.create runs only a static printf of the negative capability declaration; shell completion/output proves the active V2 plugin propagates `gentle-ai.opencode-relay/v2-staged` into ordinary host shell children. This establishes activation and that negative shell hook, not subagent dispatch or reviewer admission. Catalog response location/data shape is also verified.
- RED/GREEN: five deterministic unit tests cover cold inventory, builtins alongside managed IDs, failed activation, missing activation timeout, and duplicate identities. The initial fixture probe used an invalid location query (400); the host's private working-directory binding is now asserted directly. A subsequent strict-equality assertion timed out despite all five active because builtin plugins are also listed; a RED unit test drove subset handling. A cold-empty catalog assertion also failed after activation populated builtin model metadata; the smoke now checks location/data structure without making an empty-catalog claim.
- Exact final commands passed: `python3 -B -m unittest discover -s scripts -p test_opencode_v2_host_test.py` (5 tests); `python3 -B scripts/test-opencode-v2-host.py /private/tmp/ocv2-conformance.gt5I3r/binary/package/bin/opencode /private/tmp/ocv2-conformance.gt5I3r/node_modules` (both scope PASS lines and explicit remaining-proof notice); `git diff --check`; `git diff --exit-code -- bench`. CI now includes poll unit tests; no remote CI execution. No production asset/Go edits in this follow-up, so prior exact SDK and package verification remain applicable.
- T6 remains unchecked. Precise next step: use the already isolated persistent host with a loopback scripted provider to exercise actual foreground subagent hooks, structured final raw output, before-hook review refusal, inherited-instruction sentinels and tool/permission boundaries. Do not equate setup/shell proof with organic review conformance; advertisement stays off. No external model requests, global install, ambient credentials, user-home writes or commits. Parent owns mirror. `skill_resolution: paths-injected`.

### Accepted V2 branding limitation
- User explicitly accepted temporary absence of the Gentle logo in V2, preserving V1 branding. No alternate placement is authorized. This resolves the logo product choice only; other community-plugin compatibility and organic review proof remain separate. Memory #19727.

### T6 follow-up — Actual foreground dispatch and negative review under loopback guard
- Added deterministic `scripts/opencode_v2_loopback.py` and optional `--loopback <native-gentle-ai>` mode to the persistent host harness. The scripted OpenAI-compatible SSE endpoint runs on 127.0.0.1; native provider configuration, agents, observation plugin, instructions, auth, dependencies and logs exist only in fresh canonical temp fixtures. It does not call an external model or return an invented native approval.
- Both independent global/project fixtures passed actual 2.0.4 foreground subagent dispatch: one before/after pair in order with matching call ID; structured child status completed; exact raw output `RAW_CHILD_SENTINEL`; no background/reused session inputs. Actual provider request bytes contained child-agent and project-instruction sentinels but not parent-agent system text. The deny-all child had no tools in the context hook or provider request. This proves transport shape and inherited context, not that a real model would obey precedence or produce a valid review.
- Negative proof uses a real local build (`TMPDIR=/private/tmp go build -o /private/tmp/ocv2-conformance.gt5I3r/gentle-ai ./cmd/gentle-ai`), copied into each fixture. An actual shell invocation of the internal relay refused with `immutable_review_transport_unsupported`. A scripted parent then requested review-risk: the managed before hook returned `opencode_review_transport_relay_refused` to the parent, without any review child context or additional child-provider request. No native review was started on the real worktree, no binding/admission semantics changed, and no positive capability toggle was introduced.
- Network isolation is enforced by a per-process macOS sandbox profile denying network except localhost inbound/outbound, plus a strict trap proxy and session HTTP-request origin guard. The fixture fails on unexpected targets. It refuses loopback mode when this verified per-process mechanism is unavailable rather than claiming a local URL alone isolates networking. This proves nonloopback sends are OS-denied; it does not claim a complete audit of every denied low-level attempt. No global networking setting was changed.
- RED/GREEN evidence: new scripted responder tests initially failed because the helper did not exist. First actual runs correctly failed on an attempted CONNECT to models.opencode.ai (catalog refresh, not a model request). Pinned `packages/cli/src/server-process.ts` lines 95-99 exposes OPENCODE_MODELS_URL; the fixture now points that catalog to its own `/catalog/api.json` and keeps the strict trap. No network assertion was relaxed. A direct native-shell check then exposed the fixture's 5ms timeout; pinned `packages/core/src/shell.ts` line 362 confirms milliseconds, so the test uses 5000ms. Final both-scope runs passed with no trap failures.
- Exact final checks passed: `python3 -B -m unittest discover -s scripts -p test_opencode_v2_host_test.py` (11 tests); `python3 -B scripts/test-opencode-v2-host.py /private/tmp/ocv2-conformance.gt5I3r/binary/package/bin/opencode /private/tmp/ocv2-conformance.gt5I3r/node_modules --loopback /private/tmp/ocv2-conformance.gt5I3r/gentle-ai` (both activation and dispatch/refusal PASS lines); `TMPDIR=/private/tmp go test ./internal/assets ./internal/opencode ./internal/components/telemetryruntime` (6.086s, cached, cached); `git diff --check`; `git diff --exit-code -- bench`. No production TypeScript/Go edits in this slice, so released asset typechecking and prior affected SDD/CLI proofs remain applicable. CI executes the expanded unit tests, not the Darwin-only organic fixture.
- T6 remains unchecked: positive native reviewer transport/admission is still unadvertised and not certified. The positive bytes here come from an ordinary fixture subagent plus test-only observation; review-risk is deliberately refused. Inherited project instructions are confirmed for the ordinary child and must not be described as isolated reviewer context. Remaining conformance/delivery scope includes the supported-runtime matrix, positive review contract decision/proof, integrated checks and honest capability documentation. User-approved V2 logo omission is independent and does not authorize dropping other capabilities.
- Rollback: remove loopback helper/mode and its responder/network tests, retaining the earlier activation harness. All per-run servers/process groups and roots are cleaned; explicit pinned input fixture/build remain under /private/tmp for parent verification. No user-home installation/config mutation, global install, remote CI, commits or external inference. Parent owns task mirror. `skill_resolution: paths-injected`.

### T5 accepted logo omission and candidate freeze
- Implemented only the user-approved V2 logo omission: a logo-specific typed unsupported outcome reaches the existing pipeline as `skipped`, not installed/succeeded. The runner continues without rollback for this explicit outcome, retains its reason, and keeps ordinary errors fatal. Install, selected-plugin install and sync share the existing error propagation; sync records no changed files. TUI progress terminates with a skipped label/count instead of a success claim. V1 behavior is unchanged; unknown runtime and unproven community plugins still refuse.
- Strict RED: `TMPDIR=/private/tmp go test ./internal/cli -run TestOpenCodeV2LogoSkippedWithoutWrites -count=1` failed with failed (not skipped) status. Runner/terminal-progress tests failed before support; the screen test failed on misleading “All 0 steps completed successfully.” GREEN: `TMPDIR=/private/tmp go test ./internal/components/opencodeplugin ./internal/installcmd ./internal/pipeline ./internal/tui/screens`; `TMPDIR=/private/tmp go test ./internal/cli ./internal/tui -run 'Test(OpenCode.*(Logo|Runtime)|RunnerSkip|Progress|Installing)' -count=1`. All fixtures use temporary homes and fake version evidence; no package manager or real installer invocation.
- Added `docs/opencode-compatibility.md` distinguishing retained V1 behavior, verified V2 contracts/ordinary-child transport, accepted logo omission, unproven community plugins, unavailable native review, and pending matrix/integrated checks. The ordinary inherited-context child is not represented as an isolated reviewer or admitted review.
- Source frozen for parent integrated verification (T7). T4/T6 remain partial and unchecked; no positive capability enabled. `git diff --check` and unchanged bench check pass. No commits, home mutation or global installation. Parent owns mirror. `skill_resolution: paths-injected`.

### T7 — Integrated functional checks, native review pending
- Independent verification used disposable HOME/USERPROFILE/XDG/TMP/build caches and an allowlisted environment, GOPROXY=off/GOSUMDB=off; no installers, ambient credentials or user-home mutations.
- Initial `go test ./...` FAILED after 611.24s: CLI hit cumulative default 10-minute timeout and two update release-security groups selected Bash 3.2. All other packages passed, including reviewtransaction 443.107s. HEAD scripts reproduce the Bash errors unchanged; no source workaround made.
- Authorized targeted `go test ./internal/cli -count=1 -timeout=30m` PASS 974.883s; two exact failed update groups with existing Bash 5.2 PASS 0.450s. Timeout-active validator individually PASS 4.324s. Do not rewrite initial full-suite result as success.
- `go vet ./...` PASS 2.33s; Python fixture unit tests 11 PASS 1.23s; `git diff --check` and unchanged bench PASS. Parent pipeline skip spot-check PASS 0.004s and Python 11 PASS 1.021s. Independent bounded structural checks found no blocker in config preservation, telemetry bounds, V2 negative capability or explicit logo omission.
- Logs: /private/tmp/ocv2-integrated-mhg_w24g; observations #19743/#19746. Positive native V2 review/admission and broader runtime matrix remain incomplete. Native review for this candidate is next, not already approved; no commits or delivery authorization.

### Authorized rebase — 2026-09-18
- User authorized rebasing draft PR #4728 only, without merge or remote mutation. Backup: `backup/opencode-v2-pre-rebase-20260918` at `9abc095860ae2d977e59222322413f14deadc2b2`.
- Exact new base: `e7729359fd9d6cb691ed2a88e8f72b1372f7c92e`. Resolved eight import-only conflicts and normalized 24 feature Go files to upstream module-v3 imports, preserving both upstream and feature behavior. CodeGraph status failed (database unavailable); narrow conflict inspection is the fallback.
- Previous native approval/burn applied to the pre-rebase candidate, not this rewritten candidate. Final focused package group passed (SDD 116.495s; TUI 2.469s; all other named packages passed); focused CLI passed 58.354s; `go vet ./...`, Python fixture tests (11, 1.026s), working/staged diff checks and no-unmerged/marker checks passed.
- Integration-only test adjustment: the upstream standalone plugin registration test now supplies the existing scoped fake V1 version seam (observed RED before correction), avoiding ambient binary dependence. No production behavior repair was required. Initial sandbox loopback failures passed after authorized socket access; a restrictive harness umask caused an ownership-fixture mismatch and passed after normalizing only the test process umask inside private temporary roots.
- Verification used the exact parent-requested package and CLI filters with `-count=1 -timeout=10m`, allowlisted HOME/XDG/GOCACHE, read-only existing dependency use with `GOPROXY=off`, and existing Bash 5. No downloads, global installations, external inference or remote mutation. Rebase completion and range-diff readback follow these checks; parent owns native candidate assessment and any push.

### Authorized follow-up — CI repair and V2 beta presentation
- User authorized fixing draft PR #4728 CI and showing `(beta)` beside the installer-facing OpenCode V2 integration name. Canonical identity, binary/config keys and V1 labels remain unchanged; this labels integration maturity, not the upstream release channel.
- [ ] T8: Repair diagnosed CI failures with bounded regression coverage and applicable checks; diagnosis pending from the read-only mapper.
- [x] T9: Add evidence-bound `OpenCode V2 (beta)` installer presentation; verify V1/unknown labels remain unchanged and no new runtime backend is invented.
- Current candidate `4d3b622b` previously reached native approval and acknowledgement/burn. That historical approval does not apply automatically after these edits. PR remains draft; prior merge intent remains subject to all applicable gates. No commits, push, native lifecycle or issue mutations in this work unit. Strict TDD remains enabled by `openspec/config.yaml`.
- T9 implementation: one bounded asynchronous version observation feeds presentation-only state; the agent selector row reads `OpenCode V2 (beta)` only with V2 evidence. A supplemental `OpenCode V2 (beta): this integration is being tested.` note is visible before installation even with unknown/fresh runtime, without relabeling its identity. V1/unknown row labels and canonical selections are unchanged; `@latest` is not treated as major evidence. RED was the missing presentation command; GREEN covered fresh/V1/V2/unknown, one probe and no render-time process work.
- T8 local RED reproduced the missing-version failure in app sync, two OpenCode golden fixtures and eight organic policy-hardening cases. Scoped fake V1 evidence repairs test setup only; the organic subprocess journeys use a temporary version-only executable, not a production capability bypass or real-host substitute. App focused GREEN 0.548s; golden focused GREEN 0.951s; organic hardening plus trace-command regression GREEN 7.591s. Existing golden files were not regenerated.
- Ubuntu real-host tracing now invokes `strace -ff` once around the real top-level binary and leaves the real OpenCode on PATH, avoiding recursive tracing of relay version probes while retaining descendant connect logs. Command-shape regression had observed RED then GREEN. Actual Linux ptrace/runtime execution is pending CI, not certified by the command-shape test.
- Arch remains unresolved: no available local container daemon. Added an offline (`--pull=never --network none`) disposable-HOME container diagnostic to show actual OpenCode version stderr and exit before E2E. No image build, daemon startup or remote CI run performed here. T8 remains unchecked until actual CI outcomes resolve these environment-specific failures.
- T8 Arch follow-up: CI run `35350870795`, job `105618539929`, established that `/usr/sbin/opencode` exits 1 because the package postinstall was not run. The installed wrapper itself prescribes `cd node_modules/opencode-ai && node postinstall.mjs`; the reason installation omitted that step is still unknown. `e2e/Dockerfile.arch` now runs that published repair from the global package directory immediately after the pinned install, then requires the real `opencode --version` to equal `1.18.10` before proceeding.
- Local strict RED: new provisioning regression failed on the missing repair/version sequence. GREEN: focused Arch and organic pin contract tests passed (0.005s); the extracted RUN shell passed `sh -n` without execution. No package installation, container build or daemon startup occurred locally. Actual image/E2E outcome remains pending CI, so T8 stays unchecked. The prior Organic Ubuntu CI job now passed, confirming the top-level tracing repair on its intended platform.
- T8 benchmark follow-up: the same CI run passed `go test ./...` but its driven portable corpus failed 14 journeys (42 completed / 0 unsupported / 14 failed) because policy fixtures supplied no usable OpenCode version. The bench-only native harness alias now answers exactly `opencode --version` with synthetic V1 evidence and rejects every other invocation; explicit journey PATH overrides retain precedence. j2138 uses that fixture instead of its no-output stub. No production capability, journey assertions, real-runtime fixtures or navigation were changed.
- Strict RED: new fixture regression failed because the helper did not exist. GREEN: isolated `go test ./...` in `bench/` passed (4.731s); `go vet ./...` and both CI-shaped builds passed. Basename/platform handling, unexpected arguments and explicit PATH precedence are covered. Actual driven command: `gentle-ai-bench run --binary <isolated-built-gentle-ai> --out <isolated>/bench-portable.json`; result **55 completed / 0 unsupported / 1 failed**, exit 1. All 14 targeted failures completed. Remaining j127 timed out waiting for its Review and Confirm screen while observing Install Plan; a focused replay failed identically.
- Read-only causality comparison built the pristine `44bf8b0b` bench harness and ran j127 against the exact same unchanged product binary and isolated environment. Baseline and modified harness failed identically (no PTY output for 10s), so this failure is not introduced by the version fixture. No speculative navigation repair was made. Evidence: `/private/tmp/ocv2-rebase-check.fAtawC/bench-portable.{json,log}`, `bench-j127.json`, and `/private/tmp/ocv2-bench-baseline.1wkWOo/j127.{json,log}`. The full local driven run is NOT green; intended CI execution and Arch image/E2E remain pending, and T8 stays unchecked.
- Combined Arch/bench candidate is source-frozen for parent readback and applicable gates. Final `git diff --check` passed. Rollback boundaries are the Arch provisioning sequence plus its contract regression, and the bench-only version fixture/main dispatch/sandbox wiring/j2138 fixture replacement plus tests; neither requires reverting production V2 support. No commits, pushes, installations, Docker startup or native review were performed. Parent owns the full task mirror. `skill_resolution: paths-injected`.

- T8 final CI follow-up: run `35353502896` passed all platform tests including Arch, full Go tests, portable corpus 56/0/0, transitions 57/0/0, provider 1/0/0 and picker fixture 1/0/0 (completed/unsupported/failed). Its final deadcode ratchet failed on three newly unreachable functions. Authorized scope is removing those obsolete internal wrappers/helpers and adapting tests to the production entrypoint/explicit major; baseline growth and capability changes are excluded. Prior native approval for `8aa` does not cover this new candidate.
- Deadcode correction removed the three obsolete helpers without changing the baseline; retained `Reconcile` forwards directly with explicit V1, and tests use production entrypoints. Isolated affected-package verification passed: OpenCode 0.305s, SDD 124.462s, telemetry runtime 1.719s (`go test ./internal/opencode ./internal/components/sdd ./internal/components/telemetryruntime -count=1 -timeout=10m`). The exact unchanged deadcode ratchet passed using its pinned tool downloaded into a disposable dependency cache. Subsequent worker bookkeeping stalled, not the tests; parent recovered the observed results and preserved source. Current candidate still requires review/publication and fresh CI before T8 completion.
