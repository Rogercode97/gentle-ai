# Proposal: simplify SDD runtime interpretation

## Decision and intent

gentle-ai#4484: remove contradictory refusals through one native-selected action; Pi never chooses transitions.
Basis: [admission r8](preproposal.md#current-parent-admission), [evidence](evidence.md), [research conclusions](research.md#five-concrete-simplifications-and-deletion-proofs).
Research/write/permanent-block reports and #4470 remain symptoms/acceptance targets, not reproduced causes.

## Scope: smallest systemic correction

**Immediate mismatches**

- Align Pi with AI's seven dependencies/four instruction groups; delete exact-three assumptions/prose routing.
- Remove unrelated launch/review/delivery gates from read-only status and contradictory Engram dispatch bans.
- Align research declarations, actual tool names/grants/provisioning; carry backend/path and already-authorized OpenSpec writes across phases.

**Necessary completion**

- Re-evaluate corrected facts through existing native recovery predicates, not stale blockers.
- Complete bounded typed remediation: failed-evidence revision binding, Pi execution, native settlement, fresh independent verification. Never alias apply.
- Reuse subject/evidence/accounting. Add only proven missing facts; name the failing acceptance scenario and why consolidation/plumbing cannot suffice.

## Retained guarantees; non-goals

Keep immutable common-directory ledger/history, CAS, candidate/subject/worktree/consent bindings,
one active attempt, finite truthful outcomes and capped refunds. No-diff execution is not free.
Keep strict RED/GREEN/TRIANGULATE/REFACTOR, required implementer–verifier independence,
durable planning and advisory-only RDD.

No new engine, ledger, DSL, generator, framework, compatibility layer, waiver, automatic reset,
redundant verification gate, copied/ported/extracted source or upstream dependency.

## Capabilities and affected areas

- **New:** `sdd-native-runtime` specification: existing status/action/recovery/settlement/consumer behavior, not new machinery.
- **Modified:** `sdd-orchestrator-assets`: native interpretation/authorized handoff; `sdd-research`: truthful grants/recovery.
- **AI:** `internal/cli/sdd_status.go`, `internal/sddstatus/`, `internal/assets/`, `internal/components/sdd/`.
- **Pi:** `lib/native-review-cli.ts`; managed SDD executor/provisioning/assets.

## Acceptance and measurement

Future scenarios, not results:

1. OpenSpec/Engram status needs no launch authorization; writes no authority.
2. Producer fixtures decode consistently; malformed/unknown actions refuse.
3. Actual research grants → authorized writes → corrected-fact continuation.
4. Missing tools, out-of-scope writes, stale evidence and wrong worktrees still refuse.
5. Bound remediation reaches independent verification; unsupported consumers refuse without invented commands.
6. #4470 historical replay reconciles; fresh selections/drift remain strict.
7. Failed/interrupted/no-diff outcomes settle finitely; lost acknowledgements reconcile without re-execution/double accounting; unknown effects stay unknown.
8. Managed-child execution proves uptake/strict quality; self-report/process-success never replaces verification.

Measure removed duplicate decisions and scenario coverage.

## Risks, dependencies and rollback

Pi #593/#605/#608 uptake remains unproven; assume no merges.
Design sets schema/compatibility floor; fix v2 where sufficient.
Rollback producer/consumer/assets together; preserve history/budgets/consent.
Refuse execution if rollback cannot safely read retained records.

## Minimal sequence

Specify/design boundaries → plan immediate fixes, necessary completion/uptake → tasks → **maintainer review before implementation**.
Planning only; no execution/delivery. Retain preproposal's delivery constraints, including no tracker-aggregate size exception.
